---
change: safety-and-1-0-api-surface-canonical-addresses-strict-mnemonics-checked-amounts-an-internal-encoder-and-hash-client
artifact: tasks
---

# Tasks

- [x] T1 Create the workspace at base `4665470` with `--path Sources/Algorand --path Tests/AlgorandTests --path specs/algorand --path Package.swift` and the research and design artifacts, and while still a draft supersede the 13 pre-existing files this change modifies against every predecessor holding them at the newest digest: 43 edges, four each for the ten Swift files held by all three archives and CHG-0001, one for `AssetTransaction.swift` (fee-model archive), and one each for the spec and requirements companions (fee-model archive). The nine edges attempted for `Package.swift` and the two DocC pages were refused ("module `algorand` is not a successor-eligible signed owner"): those are exact-only inputs, and the files were reverted (T10, T11).
- [x] T2 `Address(string:)`: canonical form only by re-encoding the decoded bytes and comparing to the input; the internal base32 decoder no longer folds case; `description` is the canonical rendering.
- [x] T3 `Mnemonic.decode`: unpack all 33 bytes and throw `invalidMnemonic` unless the 33rd is zero; run the legacy `MnemonicTests` first (6 tests, unchanged, pass), so nothing depended on lenient decoding.
- [x] T4 `AmountError`; `MicroAlgos.adding`, `subtracting`, `multiplied(by:)`, `divided(by:)`, `init(checkedAlgos:)`, `microAlgosPerAlgo`; `AssetParams.baseUnits(for:)`; deprecate `init(algos:)`, the four operators, and `toBaseUnits(_:)` with messages naming the replacements, semantics unchanged; confirm the fee-model files use no deprecated symbol.
- [x] T5 Internalise `MessagePackWriter`, `MessagePackValue`, `SHA512_256`; add `MessagePackValue.raw(Data)`; `swift build` clean.
- [x] T6 `AlgorandError.invalidURL`, `EndpointURL.parse`, both string initializers; `AlgodClient.boxURL` and `applicationBox(_:name: Data)`; `SimulateRequest.encodedForSimulate()` and a msgpack `simulateTransaction` with `Accept: application/json`; `SimulateRequestTransactionGroup.txns: [Data]` plus `init(signedTransactions:)`; `waitForConfirmation` tolerating 404 through `isNotYetKnown` with the poll order unchanged and an overflow-checked window; the internal `configuration:` seam.
- [x] T7 Remove the four force unwraps: `AlgorandConfiguration` resolves its endpoint literals through a throwing `Endpoint.resolve()` at construction and stores the URLs; `IndexerClient.searchAccounts` guards its `URLComponents`.
- [x] T8 Remove `PendingTransaction.txn` and `TransactionData`; fill `BlockResponse` with the header and `transactions: [IndexerTransaction]?`; type `IndexerAsset.params` as `AssetParamsResponse` with a deprecated `IndexerAsset.AssetParams` typealias.
- [x] T9 `Account`: `SigningKeyBox` holding `Curve25519.Signing.PrivateKey`; `init()` from `PrivateKey()`; `mnemonic()` from `rawRepresentation` without scrubbing; public API unchanged.
- [x] T10 `Package.swift`: the `ALGORAND_DOCC` gate was written and verified (`swift package show-dependencies` resolves `swift-crypto` and `swift-asn1` only without it, adds `swift-docc-plugin` 1.5.0 and `swift-docc-symbolkit` 1.0.0 with it, and a path-dependent consumer resolved without the plugin), then reverted to the base tree because `Package.swift` is an exact-only delivery input of CHG-0001 that no successor can cover; the edit is kept as `docc-gating.patch` for a follow-up after CHG-0001 is reopened and its owners corrected.
- [x] T11 DocC: `GettingStarted.md` (pin `.upToNextMinor(from: "0.3.2")`, `.product(name: "Algorand", package: "swift-algorand")`, no `MicroAlgos(algos:)`, no non-throwing configuration call, no `indexerURL!`, `Address` arguments to the indexer) and `Algorand.md` (`AmountError`, `FeeError`, `TransactionAuthorizationError` in Topics) were written, then reverted for the same reason and kept in the same patch.
- [x] T12 README (canonical addresses and mnemonics, checked amounts, simulate/confirm/box, the road-to-1.0 table, configuration `try`, test notes), root `SECURITY.md` (per-platform key storage facts, what is not guaranteed, canonical input), `documentation/SECURITY.md`, `GETTING_STARTED.md`, `QUICKSTART.md`, and `TESTING.md`.
- [x] T13 `SafetyAndSurfaceTests` (31 tests, 5 in the serialized `Transport` suite); throwing calls hoisted out of `#expect`; no deprecated symbol used; no predecessor test file touched; the manifest test was removed with T10.
- [x] T14 `fledge lanes run verify` green: XCTest 98 / 20 skipped / 0 failures; Swift Testing 128 tests in 7 suites, 0 issues; forced full recompile of `Sources/Algorand` with 0 warnings; the frozen XCTest files' deprecation warnings recorded.
- [x] T15 Linux: copy of the tree under the scratchpad, `swift:6.0-jammy` `swift build --build-tests` (exit 0), then 3 × `timeout 150 swift test --skip-build --disable-xctest`, each 128 tests, exit 0.
- [x] T16 Live, read-only, TestNet v42 (`GET`s and `POST /v2/transactions/simulate` only): `invalidURL` for `"not a url"`; simulate HTTP 200 with `overspend` for the unfunded throwaway sender; `GET /v2/transactions/pending/{unknown}` 404 then `waitForConfirmation(timeout: 1)` throwing `networkError("Transaction not confirmed after 1 rounds")`; `applicationBox(770095334, name:)` returning the box whose base64 name contains `+`, against the base tree's `%3F` URL answering the router's `Not Found`; `IndexerClient.asset(10458941)` and `block(67022363)` decoding; evidence in `testing.md`.
- [x] T17 Spec: three files added to the frontmatter; delta with the full `Public API` body minus 11 rows plus 17, `Purpose`, `Invariants` 11–15, four behavioural examples, five error rows, inventory paragraph; REQ-algorand-029 to 034; `specsync check --strict` at 384/384.
- [x] T18 Interview answered (`acceptance_criteria`, `public_contract yes`, `architecture_risk yes`); `approve --actor 0xLeif`; `check`; commit; `review --reviewer 0xLeif`; `finalize` with the spec-sync #753 build; archive commit; `fledge attest sign` per commit and `attest verify`; push branch and notes; draft PR with base `feat/fee-model`.
